const UserService = require('./../Services/UserService')

class UserView {
    static CreateU(payload) {
        if (payload === null) {
            return { error: "No encontrado" }
        }
    }

}